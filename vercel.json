{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d web ]; then cd web && npm ci; else npm ci; fi",
  "buildCommand": "if [ -d web ]; then cd web && npm run build; else npm run build; fi"
}
