# Agent context — Experience Kit AI website

When working in this repository:

- The product surface is the public site at https://experiencekit.ai. Prefer verifying claims and copy against the live site (or exports in `docs/`) rather than guessing.
- Site research and snapshots belong in `docs/`; marketing and UX copy drafts belong in `content/`.
- If `.cursor` rules are added later at the repo root, keep them aligned with this file.
